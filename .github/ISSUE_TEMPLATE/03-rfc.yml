name: Design proposal (RFC)
description: Propose a substantial design change, new connector, or API shift.
title: "rfc: "
labels: ["kind:rfc", "triage"]
body:
  - type: markdown
    attributes:
      value: |
        **Substantial designs should land as a markdown file under
        `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`.** Use
        this issue to track the discussion and link the spec once
        drafted. Small features belong in
        [`01-feature.yml`](?template=01-feature.yml).
  - type: checkboxes
    id: preflight
    attributes:
      label: Pre-flight checklist
      description: Please confirm before filing.
      options:
        - label: I searched existing issues and didn't find a duplicate.
          required: true
        - label: I reproduced the issue on the latest commit of `main`.
          required: true
        - label: I read the relevant README or spec section.
          required: true
  - type: textarea
    id: motivation
    attributes:
      label: Motivation
      description: What problem does this solve, and why does it need a design pass before code?
    validations:
      required: true
  - type: textarea
    id: design
    attributes:
      label: Proposed design
      description: A summary of the design, or a link to the draft spec file.
    validations:
      required: true
  - type: textarea
    id: crates
    attributes:
      label: Affected crates
      description: Which workspace crates does this touch? List them.
    validations:
      required: true
  - type: textarea
    id: compat
    attributes:
      label: Backwards-compat & breaking-change impact
      description: What breaks for current users? Migration path?
    validations:
      required: true
  - type: textarea
    id: drawbacks
    attributes:
      label: Drawbacks
      description: What's the cost? What new failure modes does this introduce?
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Other shapes you weighed against this proposal.
    validations:
      required: true
  - type: textarea
    id: unresolved
    attributes:
      label: Unresolved questions
      description: Open questions you'd like discussion to resolve before implementation.
    validations:
      required: true
  - type: dropdown
    id: drive
    attributes:
      label: Are you offering to drive implementation?
      options:
        - "Yes"
        - "Not sure"
        - "No"
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        ---
        **taktora is a pre-1.0 personal experiment.** There is no SLA,
        no support, and no backwards-compatibility guarantee. Responses
        are best-effort. See the [README](https://github.com/patdhlk/taktora#readme)
        for the full status statement.
