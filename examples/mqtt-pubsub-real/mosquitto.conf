# Minimal broker config for the mqtt-pubsub-real example.
# Anonymous plain-TCP listener on 1883 — no auth, no TLS. This is a throwaway
# broker bundled into docker-compose purely so the example has something real
# to talk to; do not model a production broker on it.
listener 1883 0.0.0.0
allow_anonymous true
