//! Empty stub. This crate exists solely to host integration tests under
//! `tests/` that depend on sibling workspace crates (kept out of
//! `taktora-motion-core`'s published manifest). See `Cargo.toml`.
