//! Map `log::kv` pairs to DLT verbose arguments. Stub — see Task 11.

use dlt_core::dlt::Argument;

pub(crate) fn collect_arguments(_record: &log::Record<'_>, _into: &mut Vec<Argument>) {
    // Filled in by Task 11.
}
