# taktora-replay

This crate is intentionally empty. It is reserved for the eventual replay
coordinator + replay timer service integration described in the workspace
design (section 13: "Out of scope for v1").

Do not depend on this crate from production code yet.
