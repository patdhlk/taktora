//! The `#[derive(ViewModel)]` implementation.

use proc_macro2::TokenStream;

/// Generate the `ViewModel` impl. Stub until Task 2.3.
pub fn derive(_input: TokenStream) -> syn::Result<TokenStream> {
    Ok(TokenStream::new())
}
