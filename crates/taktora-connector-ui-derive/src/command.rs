//! The `#[derive(CommandParams)]` implementation.

use proc_macro2::TokenStream;

/// Generate the `CommandParams` impl. Stub until Task 2.4.
pub fn derive(_input: TokenStream) -> syn::Result<TokenStream> {
    Ok(TokenStream::new())
}
