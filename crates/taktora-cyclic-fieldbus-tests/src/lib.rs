//! Empty stub. This crate exists solely to host integration tests under
//! `tests/` that dev-depend on sibling workspace crates without polluting
//! the published manifests (see the package `description`).
