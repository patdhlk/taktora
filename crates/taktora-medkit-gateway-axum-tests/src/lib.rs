//! Empty stub. This crate exists only to host integration tests under
//! `tests/`; see the crate description in `Cargo.toml` for why the dev-deps
//! live here rather than on the published `taktora-medkit-gateway-axum`.
