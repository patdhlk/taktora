//! Reserved for telemetry/health → ViewModel bridges (executor Observer
//! telemetry, connector health → ViewModel). No implementation in v1 — see
//! ADR_0107.

#![warn(missing_docs)]
#![deny(unsafe_code)]
