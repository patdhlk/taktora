# sphinx-needs configuration consumed via `needs_from_toml = "ubproject.toml"`
# in conf.py. Holds need-type and link-type declarations only — Sphinx-level
# configuration (extensions, theme, etc.) lives in conf.py.

[project]
name = "taktora-spec"

[needs]
id_required = true
id_length = 6

# Extra fields carried by safety artefacts. (sphinx-needs 8.x renamed
# extra_options → fields and changed the shape from list-of-strings to a
# dict-of-dicts keyed by field name, with metadata like description/schema.)
[needs.fields.asil]
description = "ISO 26262 ASIL value: QM | A | B | C | D | B(D) | C(D) | etc."
nullable = true
schema = { type = "string" }

[needs.fields.github]
description = "GitHub issue number(s) tracking this need's implementation; comma-separated for multiple."
nullable = true
schema = { type = "string" }

# Need types — directives available in RST. Extend as the spec grows.
[[needs.types]]
directive = "feat"
title = "Feature"
prefix = "FEAT_"
color = "#BFD8D2"
style = "node"

[[needs.types]]
directive = "req"
title = "Requirement"
prefix = "REQ_"
color = "#BFD8D2"
style = "node"

[[needs.types]]
directive = "spec"
title = "Specification"
prefix = "SPEC_"
color = "#FEDCD2"
style = "node"

[[needs.types]]
directive = "impl"
title = "Implementation"
prefix = "IMPL_"
color = "#DF744A"
style = "node"

[[needs.types]]
directive = "test"
title = "Test Case"
prefix = "TEST_"
color = "#DCB239"
style = "node"

# arc42 architecture types — used in spec/architecture/ pages following the
# useblocks "x-as-code" arc42 conventions
# (https://x-as-code.useblocks.com/how-to-guides/arc42/index.html).

[[needs.types]]
directive = "quality-goal"
title = "Quality Goal"
prefix = "QG_"
color = "#A4D4AE"
style = "node"

[[needs.types]]
directive = "constraint"
title = "Constraint"
prefix = "CON_"
color = "#E8C8A0"
style = "node"

[[needs.types]]
directive = "arch-decision"
title = "Architecture Decision"
prefix = "ADR_"
color = "#A6C8E0"
style = "node"

[[needs.types]]
directive = "building-block"
title = "Building Block"
prefix = "BB_"
color = "#C0A6E0"
style = "node"

[[needs.types]]
directive = "architecture"
title = "Architecture View"
prefix = "ARCH_"
color = "#BFD8D2"
style = "node"

[[needs.types]]
directive = "risk"
title = "Risk"
prefix = "RISK_"
color = "#E0A6A6"
style = "node"

[[needs.types]]
directive = "term"
title = "Glossary Term"
prefix = "GLOSS_"
color = "#D8D8D8"
style = "node"

# ISO 26262 SEooC safety-concept types. The `assumed-*` prefix marks
# work products that sit *above* taktora's boundary — illustrative inputs
# from a synthetic item HARA that the integrator validates against
# their real item. `tsr` and `aou` are taktora's own commitments and
# declarations, not assumed.

[[needs.types]]
directive = "assumed-hazard"
title = "Assumed Hazard"
prefix = "AHZ_"
color = "#E0A6A6"
style = "node"

[[needs.types]]
directive = "assumed-safety-goal"
title = "Assumed Safety Goal"
prefix = "ASG_"
color = "#D88A8A"
style = "node"

[[needs.types]]
directive = "assumed-fsr"
title = "Assumed Functional Safety Requirement"
prefix = "AFSR_"
color = "#BFD8D2"
style = "node"

[[needs.types]]
directive = "tsr"
title = "Technical Safety Requirement"
prefix = "TSR_"
color = "#BFD8D2"
style = "node"

[[needs.types]]
directive = "aou"
title = "Assumption of Use"
prefix = "AOU_"
color = "#E8C8A0"
style = "node"

# Extra link types — additional traceability relations beyond the built-in
# `:links:` option. Keyed by option name; each entry sets the outgoing and
# incoming display text. (sphinx-needs 8.0 renamed extra_links → links and
# changed the shape from list-of-dicts to a dict-of-dicts.)

[needs.links.satisfies]
incoming = "is satisfied by"
outgoing = "satisfies"

[needs.links.refines]
incoming = "is refined by"
outgoing = "refines"

[needs.links.implements]
incoming = "is implemented by"
outgoing = "implements"

[needs.links.verifies]
incoming = "is verified by"
outgoing = "verifies"

# ubCode (`ubc check`) parser configuration — directives contributed by the
# local Sphinx extensions under spec/_ext, declared so the linter does not
# report them as unknown. sphinx-needs reads only the [needs] tables above.

[parse.extend_directives.test-results-if-present]
argument = true
options = true
content = true
content_required = false
description = "sphinx-test-reports `test-file` that degrades to a note when the JUnit is absent (test_records extension, FEAT_0122)"
extension = "test_records"
