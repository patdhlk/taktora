/target
/Cargo.lock

# Process artefacts (specs, brainstorming notes, etc.) — kept locally, not tracked.
/docs/superpowers/

# Spec project build artefacts and uv venv.
/spec/_build/
/spec/.venv/

# Node deps for the spec/scripts/ tooling (mermaid validator). The
# package-lock.json IS committed for reproducibility; node_modules is not.
/spec/scripts/node_modules/

# Pharaoh ephemeral state (project tailoring at .pharaoh/project/ IS committed).
/spec/.pharaoh/runs/
/spec/.pharaoh/plans/
/spec/.pharaoh/session.json
/spec/.pharaoh/cache/

# Generated jitter plots
*.png
xtask/preempt-rt/plots/*.png
